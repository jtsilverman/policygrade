# Optional: GitHub token. Used by `skillscore index` (CLI) and the daily web-directory indexer.
# Without it, the GitHub API caps you at 60 requests/hour. With it, 5,000/hour.
# CLI grading of a single skill works fine without this.
GITHUB_TOKEN=ghp_...
